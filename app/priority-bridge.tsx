"use client";
import { useEffect } from "react";
const options=["안전이 가장 중요","사람이 적은 곳","경치가 좋은 곳","주차가 편리한 곳","대중교통이 편리한 곳","물놀이하기 좋은 곳","노을이 아름다운 곳","사진 찍기 좋은 곳","편의시설이 많은 곳","조용한 분위기"];
export default function PriorityBridge(){useEffect(()=>{const grid=document.querySelector<HTMLElement>(".activity-grid");if(!grid||document.querySelector(".restored-priority"))return;const panel=document.createElement("section");panel.className="restored-priority";panel.innerHTML=`<b>무엇을 가장 중요하게 생각하세요? <small>복수 선택</small></b><div>${options.map(x=>`<button type="button" aria-pressed="false">${x}</button>`).join("")}</div>`;panel.querySelectorAll("button").forEach(b=>b.addEventListener("click",()=>{const selected=b.classList.toggle("selected");b.setAttribute("aria-pressed",String(selected));}));grid.insertAdjacentElement("afterend",panel);return()=>panel.remove()},[]);return null}
